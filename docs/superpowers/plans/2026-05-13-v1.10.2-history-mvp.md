# v1.10.2 — 版本时间线 MVP（History Drawer）

**版本号**：v1.10.2（minor — G2 闭环最大缺口）
**起草时间**：2026-05-13 00:05
**优先级**：P1（自动迭代第 2 轮）

## Goal

让 `_auto-save/` 从"用户看不见的文件夹"升级成"产品功能"。最小可用：当前文件页面右上角加 🕐 历史按钮 → 弹抽屉显示版本列表 → 每条带时间 + 字数变化 + [👁 预览] [↩ 恢复]。

## Why

v1.8.2 roadmap 明确 G2「零数据丢失」90% 完成度，唯一短板就是"快照存在但用不上"。当前用户误覆盖只能开 Finder 翻 `_auto-save/`。

## Tasks

- [ ] T1 后端 GET /api/history?path=... 返回快照列表 + meta
- [ ] T2 后端 POST /api/restore {snapshot_path} 一键恢复（带预-恢复快照）
- [ ] T3 前端右上角 🕐 历史按钮 + drawer
- [ ] T4 drawer 列表渲染：时间 / 文件大小 / 类型徽章（自动快照 vs 覆盖前备份）
- [ ] T5 [👁 预览] 点击 → 在 iframe 里显示历史版本（只读模式）
- [ ] T6 [↩ 恢复] 点击 → 二次确认 → POST /api/restore
- [ ] T7 CHANGELOG + commit + tag
